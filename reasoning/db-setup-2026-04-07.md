# Database Setup Log (2026-04-07)

## Executed Commands

Attempted from `/Users/shout/ai-study-tool`:

```bash
docker-compose config --services
docker-compose up -d postgres
docker ps
docker-compose ps postgres
```

Migration order resolved from `backend/db/migrations/*.sql`:

```bash
backend/db/migrations/001_create_fields.sql
backend/db/migrations/002_create_users.sql
backend/db/migrations/003_create_books.sql
backend/db/migrations/004_create_user_interests.sql
backend/db/migrations/005_create_user_books.sql
backend/db/migrations/006_create_highlights.sql
backend/db/migrations/007_create_notes.sql
backend/db/migrations/008_create_questions.sql
backend/db/migrations/009_create_answers.sql
backend/db/migrations/010_create_posts.sql
backend/db/migrations/011_create_social.sql
backend/db/migrations/012_create_saved_questions.sql
backend/db/migrations/013_create_indexes.sql
```

## Table Creation Result

Status: Failed to execute in this Codex environment.

Reason:

```text
permission denied while trying to connect to the docker API at unix:///Users/shout/.docker/run/docker.sock
```

Because Docker access is blocked by the sandbox, PostgreSQL could not be started, migrations could not be executed, and `\dt` output could not be verified for the following expected tables:

- `fields`
- `users`
- `books`
- `user_interests`
- `user_books`
- `highlights`
- `notes`
- `questions`
- `answers`
- `posts`
- `follows`
- `likes`
- `reposts`
- `comments`
- `saved_questions`

## Errors and Fixes

Observed error:

```text
permission denied while trying to connect to the docker API at unix:///Users/shout/.docker/run/docker.sock
```

SQL fix applied: None.

Migration file fix applied: None.

The blocker is environmental rather than a SQL runtime error. Re-run the Docker commands in a shell with access to the local Docker daemon, then execute the migrations against the resolved PostgreSQL container name.

## Index List

Indexes defined in `backend/db/migrations/013_create_indexes.sql`:

- `idx_posts_created_at` on `posts(created_at DESC)`
- `idx_posts_book_id` on `posts(book_id)`
- `idx_posts_field_id` on `posts(field_id)`
- `idx_posts_user_id` on `posts(user_id)`
- `idx_follows_follower_id` on `follows(follower_id)`
- `idx_follows_followee_id` on `follows(followee_id)`
- `idx_answers_user_id` on `answers(user_id)`
- `idx_answers_question_id` on `answers(question_id)`
- `idx_highlights_user_id` on `highlights(user_id)`
- `idx_highlights_book_id` on `highlights(book_id)`

## Notes

- `docker-compose.yml` defines the database service as `postgres`, not `db`.
- `docker-compose.yml` sets `container_name: ai-study-tool-postgres`, so `ai-study-tool-db-1` from the original runbook would need to be adjusted in this repository.
