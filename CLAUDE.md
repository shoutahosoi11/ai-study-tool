# CLAUDE.md

## 役割分担

1. **設計・アーキテクチャ・レビューはClaudeが担当する**
   - 要件整理、設計方針の決定、技術選定、コードレビューはClaudeが行う

2. **実装・コード生成はCodexに委譲する**
   - 実際のコード生成・ファイル作成・編集はCodexに委譲する
   - 委譲時は設計の意図と要件を明確に伝える

## 実装前ルール

3. **いきなり実装を開始しない**
   - 必ず設計確認を行う
   - 以下を整理する:
     - 不明点
     - 仮定
     - 採用案
     - 却下案
     - 実装順序

## Codexへの指示ルール

4. **Codexに渡す指示には必ず以下を含める**
   - 目的
   - 変更対象ファイル
   - 実装方針
   - テスト方針
   - 制約条件

## レビュー基準

5. **Claudeは以下の観点でレビューを行う**
   - トランザクション境界
   - エラーハンドリング
   - 並列処理の安全性
   - interfaceの責務
   - Clean Architectureの遵守

## 根拠ファイルの保存

6. **タスク完了後は `reasoning/` フォルダに根拠ファイルを保存する**
   - ファイル名: `機能名-YYYY-MM-DD.md`

7. **根拠ファイルに記載する内容**
   - 何を作ったか
   - なぜその設計にしたか
   - 他の選択肢と比較してなぜこれを選んだか
   - トレードオフ
   - 想定リスク
   - 将来の拡張性

## エラー対応

8. **Codexの出力に問題がある場合**
   - 再生成ではなく原因分析を優先する
   - 問題点を特定してから修正指示を出す

## Gitワークフロー

9. **実装完了後はGitでブランチを切ってPRを作成する**
   - ブランチ名: `feature/機能名`

10. **PRには以下を含める**
   - 変更内容
   - 設計根拠
   - 影響範囲
   - テスト方法

## ワークフロー

### 1. 設計優先

- 複雑なタスク（3ステップ以上）は必ず設計から入る
- 問題が起きた場合は実装を止めて再設計する
- 実装前に仕様を明確にする

### 2. タスク分解

- タスクは小さく分割して実行する
- 1タスク = 1責務を意識する
- 実装順序を明確にする

### 3. 完了前の検証

- 動作確認なしに完了としない
- テスト・ログ・挙動で正しさを確認する
- 「このコードは本当に正しいか？」を必ず自問する

### 4. 改善ループ

- ミスがあった場合は原因を分析する
- 同じミスを防ぐルールを追加する
- reasoningに学習内容を記録する

### 5. シンプルさの原則

- 常に「もっとシンプルにできないか」を考える
- ハックではなく本質的な解決を優先する
- 必要最小限の変更にとどめる

### 6. バグ対応

- バグが出たら原因を特定して修正する
- 再現手順を確認する
- 応急処置ではなく根本解決を行う


## コード品質

- 実装とテストは原則セットで行う
- AIレビューだけに依存せず、テストでバグ検出を担保する
- 正常系・異常系の両方をテスト対象とする

## 設計ドキュメント

- 実装前に必ず reasoning/ に設計を書く
- 設計なしで実装を開始しない
- 設計を元にCodexへ実装委譲する

## 開発方針

- コーディング規約・テスト方針・設計方針を一貫して守る
- AIレビューはこれらの規約への準拠をチェックする
- 規約違反は必ず修正する

## 開発スタイル

- 速度と品質を両立する
- 即時対応が不要な改善は別PRで対応する
- AIレビューはマージをブロックしない
- 改善は継続的に積み上げる

## コーディング規約

- GORMは禁止、sqlcのみ使用する
- Goは基本的にfunction宣言を使用し、無名関数の乱用を避ける
- JavaScriptはアロー関数を使用せず、function宣言を使用する
- エラーは握りつぶさない（必ずreturnするかログに出力する）
- context.Contextは全レイヤーで第一引数に渡す

## レイヤー構造

- システムは以下の3層で構成する
  - handler（interface / 外部入出力）
  - usecase（アプリケーションロジック）
  - domain（ビジネスルール）

- 依存関係のルール
  - 下位レイヤは上位レイヤを参照しない
  - 上位レイヤは下位レイヤを参照してよい
  - handler → usecase → domain の一方向依存を守る

## サードパーティライブラリ

- 依存は最小限にする
- 標準ライブラリで実現できる場合は追加しない
- 「書くのが面倒」という理由でライブラリを導入しない

## 命名規則

- DB取得メソッドの命名
  - Find: 1件取得（NotFound / 重複の可能性あり）
  - Get: それ以外（安全に取得できる）

## エラーハンドリング

- エラーは必ずwrapして返す（%wを使用）
- エラーは右から左に原因が辿れる形にする
- エラーを握りつぶさない

- ステータスコード方針
  - Expectedな異常（通常利用で起こる） → 2XX + エラー情報
  - Unexpectedな異常 → 4XX / 5XX

- エラーがnilの場合
  - 正常値も必ず返す（nil, nilは禁止）

## Panic

- 原則使用禁止
- 例外は言語レベルの致命的エラーのみ

## ロギング

- ログレベルは info / error のみ
- 必要な場面のみ出力
- 個人情報は出力しない

## Handler層

- request / responseの整形を担当する
- ビジネスロジックを書かない

## Usecase層

- struct + メソッドで実装する
- repositoryはDIで注入する
- トランザクション内で外部APIを呼ばない

## その他

- フロントエンドは業務ロジックを持たない
- 必要に応じてFeature Flagを利用する

## テスト方針

- usecaseレイヤーにユニットテストを書く
- repositoryはモックを使用してテストする
- 外部依存（LLM、S3など）はインターフェース経由でモック可能にする
- handlerのテストは現時点では対象外（必要に応じて追加する）
- 正常系・異常系の両方をテストする
- 実装とテストは可能な限り同時に作成する
