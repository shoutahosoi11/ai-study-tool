# CLAUDE.md

## 役割分担

1. **設計・アーキテクチャ・レビューはClaudeが担当する**
   - 要件整理、設計方針の決定、技術選定、コードレビューはClaudeが行う

2. **実装・コード生成はCodexに `/codex:rescue` で委譲する**
   - 実際のコード生成・ファイル作成・編集はCodexに委譲する
   - 委譲時は設計の意図と要件を明確に伝える

## 根拠ファイルの保存

3. **タスクが完了したらローカルの `reasoning/` フォルダに根拠ファイルを保存する**
   - ファイル名: `機能名-YYYY-MM-DD.md`
   - `reasoning/` は個人用メモとしてGit管理しない

4. **根拠ファイルに記載する内容**
   - 何を作ったか
   - なぜその設計にしたか
   - 他の選択肢と比較してなぜこれを選んだか

## Gitワークフロー

5. **実装完了後はGitでブランチを切ってコミットしPRを作成する**
   - ブランチ名: `feature/機能名`
